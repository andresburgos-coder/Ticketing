/**
 * TicketType enum - Represents the different types of tickets available.
 * Following the design specification with exactly three types.
 */
export enum TicketType {
  VIP = "VIP",
  GENERAL = "GENERAL",
  EARLY_BIRD = "EARLY_BIRD",
}
